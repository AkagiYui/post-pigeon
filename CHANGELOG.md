# 变更日志

本文件是面向使用者的变更日志的唯一事实源：发版时
[scripts/extract_changelog.py](scripts/extract_changelog.py) 会生成 GitHub Release
正文（正式版汇总上一个正式版之后的全部小节），应用内的更新提示也读它（详见
[CONTRIBUTING.md](CONTRIBUTING.md#变更日志)）。由 commit 生成的详细清单折叠在
Release 正文后面备查。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- **接口树可以按住 Option/Alt 直达设置**：按住 `Option`（macOS）或 `Alt`（Windows/Linux）
  单击模块、文件夹，会直接打开对应的范围设置且不改变展开状态；用同样方式单击接口，则会打开接口
  详情并直接切到「设置」标签。普通单击、右键菜单和省略号菜单的行为保持不变

## [0.0.6] - 2026-08-30

### 新增

- **Cookie 改为模块隔离的命名会话，并支持按需共享**：每个新模块默认拥有自己的私有 Cookie
  会话，不再把登录态自动扩散到项目内所有模块。项目设置可以新建命名 Cookie 会话，并把多个模块
  显式绑定到同一会话，以满足 SSO、网关和跨服务联调；还可以针对某个环境继承模块默认值、切换到
  另一会话或彻底禁用自动 Cookie。升级时，旧项目已有的项目级 Cookie 会迁入一个共享会话，并继续
  绑定到原有模块，避免现有调试流程突然掉登录态。请求只会发送当前会话中符合目标域名、路径、
  Secure 和公共后缀规则的 Cookie

- **流式 HTTP 响应有独立 Timeline，但不再牺牲普通响应信息**：`text/event-stream`、NDJSON 和
  JSON Text Sequence 响应会在响应区实时逐条显示，同时仍保留正文、响应头、Cookie、脚本、实际请求
  等普通标签。SSE 时间线会分别展示 `event`、`id`、`retry` 和注释；可搜索、排序、跟随最新、查看原始
  协议记录，并可导出完整转录文本。正常结束、错误和手动停止都会明确显示连接已结束

- **SSE 流有可配置的资源边界与断线续接**：请求设置新增单条记录、单连接累计字节数和事件数上限，
  避免意外长流耗尽内存。可选择在 SSE 正常 EOF 后自动重连，应用会保留服务端最近的事件 ID 作为
  `Last-Event-ID`，并遵守服务端 `retry:` 指定的等待时间；该行为默认关闭，避免改变一次性事件流。
  流式时间线与后端默认一样最多保留 10,000 条，并利用浏览器可见区域延迟布局，长连接也能保持可用

- **WebSocket 可发送二进制帧并保留关闭协商信息**：消息编辑器可在文本、Base64、十六进制负载间切换，
  后两种都会作为真正的二进制帧发出（Hex 支持空格分组）。收到或主动发起关闭时，消息时间线和详情会
  显示关闭码及关闭原因，排查 1008、1011 等协议级关闭不再只能看一段错误文本

- **WebSocket 消息改为在请求区编辑并保留草稿**：WS 请求编辑的首个标签页现在是「消息」，
  可以先在未连接时编写多行消息，连接后再发送；`Ctrl/⌘ + Enter` 发送、Enter 继续换行。
  「发送后清空」可按需开关且会保存在本地，消息草稿也按接口保存，切换接口或重启应用后仍可恢复。
  响应区则专注展示收发消息和消息工具栏

- **WebSocket 消息列表可以筛选和排序**：参考 Apifox 的消息工具栏，新增按消息内容实时搜索、
  「全部 / 发送 / 接收」方向筛选和时间正序 / 倒序切换；每条消息显示收发时间，工具栏同步显示
  当前连接的消息总数，筛选无结果时也会和真正的空消息列表明确区分。搜索不区分大小写，切换
  排序不会改动消息实际到达顺序。「跟随最新」默认开启：正序时新消息自动滚到底部，倒序时
  自动滚到顶部；用户主动滚动列表后会立即关闭跟随，避免视图被下一条消息抢回。清空列表仍只
  清消息、不影响当前连接

- **WebSocket 消息支持展开详情查看**：点击消息行即可在同一面板展开可选中复制的正文，并在
  「格式化 / 原始 / 预览」间切换；JSON、XML、HTML 格式和 UTF-8、GBK、GB2312、ISO-8859-1
  编码也可按需选择，二进制帧会按所选编码重新解码。响应面板在右侧的纵向布局中详情显示在
  列表下方，上下布局中显示在右侧；切换接口或响应标签后会恢复该接口此前已选中的消息，
  清空消息记录时才关闭详情

- **WebSocket 消息可以按需解析与格式化 JSON**：消息栏新增「解析 JSON」和「格式化」两个
  独立开关。解析会把 `\u003c` 等 JSON 字符串转义还原为实际字符；只解析、不格式化时仍保留
  服务端原有的空格、换行、键顺序和大整数文本，打开格式化后才改为两空格缩进。关闭解析即可
  查看原始帧，非法 JSON 与二进制消息始终原样显示；两个选择都会保存在本地，重启后继续生效

- **WebSocket 地址可以五级继承地自动换协议头**：全局默认开启，把 `http://` 自动换成
  `ws://`、`https://` 自动换成 `wss://`；项目、模块、文件夹和接口都可以选「继承上级 / 开 / 关」，
  嵌套文件夹会从接口所在目录开始逐层向父目录查找，再依次回到模块、项目和全局。WS 接口详情
  的环境选择器也显示最终生效的 `ws://` / `wss://` Base URL，不会再出现界面写着 HTTP 地址、
  实际连接却需要 WebSocket 地址的割裂。克隆项目时各级覆盖值会一并保留

- **常用请求设置统一升级为五级继承**：代理、TLS 校验、URL 自动编码、跟随重定向、
  请求超时和自动添加 `Cache-Control: no-cache` 现在都按「接口 → 当前文件夹 → 逐层父文件夹
  → 模块 → 项目 → 全局」解析，项目、模块、文件夹和接口默认继承上级，也都可以就近覆盖。
  代理实体与 TLS 证书材料仍集中在项目和全局，低层级只保存选择，避免重复凭据；超时明确区分
  「继承 / 指定时长 / 不限制」，旧接口已有的显式超时会在迁移后保持原行为。URL 编码的三个档位是
  **遵循 RFC 3986**（默认，行为与从前一模一样）、
  **遵循 WHATWG**（少编码 `` < > " ` { } | \ ^ ' `` 这几个字符，有些服务端的签名算法依赖它们保持原样）、
  **关闭自动编码**（路径与 Query 里的中文、手写的 %XX 都原样发出去）。
  关掉之后仍会编码那些「留着就发不出去」的字符：空格、控制字符，以及会把 URL 拆错的
  `& = # ?`。导出的 cURL 命令跟着同一套编码走，复制出去跑到的还是同一个地址

- **项目设置里可以克隆和删除项目**：「项目设置 → 通用」下多了这两项。
  克隆是完整克隆——挂在项目下的每一张表都复制一份：模块、目录、接口（含参数、请求头、
  认证、响应示例与定义、前置后置操作）、环境与变量、模块变量、全局变量、脚本库、
  项目级代理与 TLS 设置，以及 Cookie、上次响应与请求历史。Cookie 跟着走意味着克隆件
  不必重新登录一遍，接着调后续接口就行。弹窗里可以先改新项目名（默认「原名 副本」）；
  跨表的引用会一并改指到克隆出的那份——引用脚本库的前置后置操作认新脚本、模块前置 URL
  认新环境、请求历史认新接口，不会串回源项目。删除要求把项目名原样敲一遍才放行——
  过去删项目只在首页点两下就没了，而设置页离「保存」很近，误触的代价太大。弹窗里的
  提示文本（含那行要照着敲的项目名）可以选中复制，不用手抄。删除后会关掉它在顶栏的
  标签页并跳去其它已打开的项目

- **Windows 内置浏览器内核版**：新增 `-fixedwebview.zip`（绿色免安装）与
  `-fixedwebview-installer.exe` 两个产物，把 WebView2 运行时随包带上，不装、不写
  注册表、不需要管理员权限。给的是那些装不上也留不住 WebView2 的机器：精简版 /
  Ghost 版系统、网吧还原卡与无盘环境、离线内网机——过去这些机器上应用双击了没反应，
  现在解压即用。和常规版是同一个二进制，应用目录下有 `webview2\` 文件夹就用它、
  没有就退回系统的那份，因此常规版遇到运行时损坏时把文件夹拷进去也能自救。
  自动更新仍走同一个 `.exe` 资产，只换程序本体、不重下几百 MB 的内核。
  用环境变量 `POSTPIGEON_WEBVIEW_PATH` 可以指定运行时目录或强制走系统内核，
  `POSTPIGEON_WEBVIEW_ARGS` 可以追加 WebView2 启动参数（例如精简版系统上
  GPU 栈异常导致黑屏时加 `--disable-gpu`）
- **「设置 → 关于」显示浏览器内核信息**：内核名称与版本、来源（随包内置 / 系统安装）、
  内置内核的目录、完整 User Agent，都可点击复制。用户报「白屏 / 渲染不对」时，
  这一栏比什么都直接

- **默认 User-Agent**：「设置 → 请求」里可以填全局默认的 User-Agent。留空就发
  `PostPigeon/1.0.0 (https://github.com/AkagiYui/PostPigeon)`，不再用 Go 那个
  `Go-http-client/1.1`——有些站点见到它会直接拦掉或返回和浏览器不一样的内容。
  接口自己在请求头里写了 User-Agent 时以接口为准；在接口上把 User-Agent 的值留空，
  表示这个请求不带该头，全局默认值也不会补上去
- **模块变量**：模块右键「设置」里多了「模块变量」一页。它是作用域缩小到单个模块的
  全局变量——跨环境生效，但只对该模块下的接口可见，适合放各服务自己的 host、token
  这类不该污染整个项目的值。同名时的覆盖顺序是「环境变量 > 模块变量 > 全局变量」；
  可以标记为秘密变量（值默认打码，写进请求历史时一并脱敏）。脚本里用
  `pm.moduleVariables.get/set/unset` 读写，改动会在请求结束后存回该模块。
  从 Apifox 导入时，导出文件里的模块变量会一并带过来
- **首页「导入项目」改用导入弹窗**：和接口导入用的是同一个向导，于是同样支持
  本地文件（可拖入）、URL、粘贴文本三种来源。可选格式为本应用自己的项目导出文件，
  以及 **Apifox——直接导入成一个新项目**，导入前可以改项目名（默认填好导出文件里的
  项目名）、也可以只勾选要导入的接口。建项目与导入在同一个事务里，导入失败不会留下空项目

### 变更

- **正式版更新日志统一与上一个正式版比较**：GitHub Release 的用户说明和折叠的提交清单
  现在共用同一个语义化版本区间。正式版会汇总上一个正式版之后的全部 CHANGELOG 小节，
  beta / rc 不会再被误当成基线而漏掉预发布阶段的内容；只有预发布版才与上一个语义化版本比较
- **应用内更新日志更容易扫读**：跨版本升级会显示涉及的版本数和用户可见变更总数，预发布版本
  有独立标记，所有待升级版本默认展开。远端 CHANGELOG 资产暂时取不到时，Release 正文也会按
  Markdown 正常显示，并自动隐藏只供开发排查的 commit hash 附录

### 修复

- **WebSocket 连接中切换接口再切回来，响应区显示成“未请求”**：WS 连接与消息本来仍在后台
  正常保持，但切换接口会清掉当前页面的握手响应，响应区因此错误退回占位状态，只能断开重连
  才重新显示。现在会按接口保留握手响应，切回来时完整恢复消息、响应头、Cookie、脚本与实际
  请求等标签，之前收到的消息和仍在进行的收发也都可以继续查看
- **WebSocket 握手不再绕过普通请求的编辑与响应能力**：WS 接口现在会和普通请求一样解析
  环境变量、模块变量、全局变量、路径与 Query 参数、请求头、Cookie、继承认证和前置后置操作，
  代理、TLS、超时、重定向、URL 编码、默认 User-Agent 与项目 Cookie Jar 也会照常生效。
  响应区只把「响应正文」换成 WS 消息收发，其余标签仍可查看 101 握手或 4xx/5xx 拒绝响应的
  响应头、Cookie、脚本输出和实际请求；实际请求会包含自动生成的 Upgrade 握手头，排查
  Origin、认证或反向代理导致的 400 时不再只能看到一条连接错误
- **清空 WebSocket 消息后界面显示已断开**：垃圾桶过去把消息和连接状态一起清掉，
  后端连接虽然还活着，发送框和断开按钮却会立即失效。现在清空只删除消息记录，连接状态与
  后续收发不受影响；SSE 事件流的清空按钮也采用相同行为
- **第一次打开接口详情时点不开环境选择器**：接口详情与环境地址现在会在同一次加载中准备好，
  不必关闭重开或切换第二个接口后才能选择环境；快速连续切换接口时，较早返回的加载结果也不会
  覆盖当前接口。新建但尚未配置 Base URL 的环境同样会出现在选择器中
- **接口选「无认证」仍会带上模块或文件夹认证**：「无认证」过去和「没有保存认证配置」
  共用同一个空值，而后者的含义又是「继承上级」，所以保存或直接发送后都会重新拿到
  上级的 Bearer、Basic、API Key 等认证。现在 `无认证` 会作为明确的接口设置保存，
  和「继承上级」彻底分开；旧数据里没有认证记录的接口也会按真实行为显示为「继承」
- **改完接口设置、没点保存就发送时仍使用旧配置**：代理、TLS、URL 编码切回「跟随上级」
  时不再回落到数据库里的旧接口覆盖值；认证、模块参数开关、前置后置操作及其继承开关
  也都以界面当前编辑态为准。复制为 cURL 与直接发送沿用同一份当前设置
- **显式关闭值在保存或复制后重新开启**：禁用的项目全局变量不再被数据库默认值改回启用，
  新建或克隆接口时的「不继承上级操作」也会保留。复制接口、文件夹或模块现在会完整带上
  模块/文件夹认证、接口代理与 TLS、URL 编码、认证、操作继承开关、模块参数和模块变量，
  不会让复制件悄悄退回默认继承链

- **项目设置左侧「证书」「Cookie」两个标签显示成 `tls` `cookies`**：标签文案表里漏了
  这两条，代码退回去显示原始的 key。补上了

- **导入弹窗里把文件拖进去没反应**：WebView 默认不会把系统拖来的文件交给页面，
  macOS 上更是被原生层整个截走，页面的 drop 事件从头到尾都不会触发。现在开启了
  Wails 的文件拖放，拖放区落到后端拿路径再读取文件内容，拖上来时也会高亮
- **每次启动都提示「上次没有正常退出」**：清除运行标记的代码写在 `app.Run()` 之后，
  而 macOS 的退出走 `[NSApp terminate:]`，AppKit 会在跑完退出钩子后直接结束进程，
  `app.Run()` 根本不会返回——那段清理从来没执行过，标记于是永远留着。现在改挂到
  Wails 的 shutdown 钩子上，三个平台一致。被强杀时钩子不执行，仍能正确报出异常退出
- **Apifox 导入丢掉空文件夹**：文件夹原本是随接口按需创建的，一个接口都没有的目录
  就整棵被跳过了，而这类目录多半是「占位待填」的结构，导进来凭空少几个只会让人以为
  导漏了。现在会一并补建（含嵌套）。勾选式导入时只补在确实导入了内容的模块下
- **重复导入 Apifox 文件会把内容整份翻倍**：接口、目录、参数、变量、脚本库过去都是
  「再加一份」，导两次就得手工清一遍。现在改成合并，同一份文件导多少次结果都一样：
  已存在的接口按「同目录 + 同方法 + 同路径」原地更新（文档按名称），模块、目录、环境、
  各类变量与脚本库按名称合并。接口原地更新意味着 ID 不变，挂在上面的请求历史与已保存的
  响应不会失联；重新导入也因此能把上游的改动同步下来。代理、TLS、超时这些 Apifox 侧
  没有的本地设置会保留
- **导入时可以选「什么算同一条接口」**：Apifox 与 OpenAPI 的导入框里多了「重复判定」
  一项，可选按来源 ID、按方法 + 路径、按方法 + 路径 + 名称。来源 ID 指 Apifox 的接口 ID
  或 OpenAPI 的 operationId，改名、改路径、挪目录都认得出是同一条；同一路径下挂着多条
  只有名字不同的接口时，带名称那档最不容易串位。Apifox 默认按来源 ID（导出必带接口 ID），
  OpenAPI 默认按方法 + 路径（operationId 是可选字段，很多导出并不写，没写的会自动退回）。
  接口的来源标识会记进库里，升级前导入的老数据在第一次重新导入时被认领并补上
- OpenAPI 导入的重复判定口径从「方法 + 名称」改成「方法 + 路径」。名称取自 summary，
  上游改一句描述就会重复建一条；而方法 + 路径是 OpenAPI 文档的结构主键
- **窗口全屏状态的监听不会解绑**：标题栏订阅了 macOS 的全屏事件（其它平台是一个
  500ms 的轮询），但注册清理的那段代码写在一次异步读取之后——那时组件的响应式作用域
  已经结束，清理从来没真正挂上去，监听器和定时器于是一直留着。现在改成同步注册；
  订阅也提到了首次读取之前，启动那一瞬间切全屏不会再被漏掉

## [0.0.5] - 2026-08-28

### 新增

- **导入接口收拢成一个弹窗**：接口树顶部的「导入接口」现在一处选格式（Postman /
  Apifox）、一处选来源——本地文件（可直接把文件拖进来）、URL，或者粘贴一段文本。
  从 URL 拉取由应用自己发起，不受 WebView 的跨域限制，粘贴文本则可以一键取剪贴板内容
- **OpenAPI / Swagger 文档可以从项目层面导入了**：同一个「导入接口」弹窗里多了
  OpenAPI 这一档（OpenAPI 3 与 Swagger 2 都能读），并且可以选把接口并进某个已有模块，
  还是新建一个模块（模块名留空就用文档标题）。模块右键的「导入接口文档」也走这个弹窗，
  只是格式与目标模块已经替你定好，于是那里同样多了从 URL 与粘贴文本导入

## [0.0.5-beta.1] - 2026-08-27

### 其它

- **这是一个只用来验证发布流程的测试版本**：相比 0.0.4 没有任何面向使用者的改动。
  这次改的是构建链路本身——把 GitHub Actions 全部升到最新大版本，并修掉了发布脚本
  里三处遇到含空格的文件名会出错的写法。发出来是为了让 macOS 签名、Windows 打包与
  校验和生成这几段真正跑一遍

## [0.0.4] - 2026-08-27

### 新增

- **参数与请求头支持批量编辑**：表格右上角切一下，整张表摊成 `name: value` 文本，
  从别处复制来的一串参数可以整段粘进去，行首加 `//` 表示停用该行
- **导入文件允许带注释**：导入 OpenAPI / Swagger、Postman、Apifox 文件时，文件里带
  `//` 注释或末尾多余的逗号不再解析失败——从文档或配置仓库里复制出来的 JSON 常是这样
- **JSON5 一键转标准 JSON**：从浏览器控制台或 JS 代码里复制来的对象字面量（单引号、
  不带引号的键名、十六进制数字）可以在请求体上方一键转成标准 JSON，不用手改
- **请求体编辑器换成了代码编辑器**：JSON / XML / 纯文本请求体现在有语法高亮、行号与
  代码折叠，注释按注释高亮；JSON 与 XML 还多了一个「格式化」按钮，格式化 JSON 时
  注释会留在原处、大整数也不会被改写
- **JSON 请求体可以写注释了**：JSON 请求体里可以直接写 `//` 与 `/* */` 注释、以及
  末尾多余的逗号（JSONC），发送请求和导出 cURL 之前会自动去掉——注释只留在编辑器里，
  服务端收到的仍是标准 JSON。只有「去掉之后能得到合法 JSON」时才会改写，已经写对的
  请求体一个字节都不会动。不需要的话可以在设置 →「请求与历史」里关掉
- **导出项目前会问一句要不要带凭据**：项目里有秘密变量或接口鉴权凭据时才弹窗，
  默认不带。不带时变量名与鉴权配置照样保留，只是值留空，对方导入后自己填——
  把接口集合发给同事不用再担心顺手把自己的 token 也发出去
- **更新日志按 Markdown 渲染**：更新提示与「关于 → 更新日志」里的加粗、行内代码、
  链接都会正常显示，不再是一串 `**星号**`
- **「关于」里可以直达历史版本**：新版本用着不顺手时，从这里下载任意旧版本装回去
  即可，数据不受影响；同时补上了源码仓库入口
- **异常退出会被记下来，并能导出诊断信息**：上次没能正常关闭时启动会给出提示；
  设置 →「数据」里可以导出一个诊断压缩包（版本、平台、数据库体积、最近几天的日志），
  反馈问题时带上即可。包里不含数据库文件。崩溃堆栈现在也会写进日志
- **数据目录、导出与恢复**：设置 →「数据」里可以直接打开数据目录、把整个数据库导出
  成一个文件（换电脑时带走），也可以从升级前的自动备份或自己导出的文件恢复——恢复
  在重启后生效，被覆盖的那份会另存为备份
- **大附件不再整个读进内存**：带附件的请求改为边读边发，几百 MB 的文件也只占很小的
  内存；重定向导致重发时也会重新从文件读一遍
- **请求体里的附件改为记录文件路径**：选择文件走系统的文件选择框，数据库里只存路径，
  发送时才去读——附件不再被塞进数据库，库体积、备份与导出都跟着瘦下来。代价是文件
  被移动、删除或换了台机器后需要重新选择，发送时会明确提示是哪个文件读不到。
  已经存在的旧附件仍然照常发送，不受影响
- **数据库体积可见、可压缩**：设置里新增「数据」一页，能看到数据库有多大、有多少
  空间可以回收，一键压缩把清理历史、删除项目腾出来的空间还给磁盘
- **升级前自动备份数据库**：新版本需要改动数据库结构时，会先把整个库快照成数据
  目录下的 `postpigeon.db.bak-<时间>-<版本>`，只保留最近 3 份。备份做不出来
  （比如磁盘满了）就不会继续升级，数据保持原样
- **全局请求超时**：设置 →「请求与历史」里可以给所有请求设一个统一的超时时间
  （毫秒）。接口自己填了超时仍以接口为准；这一项留空表示不设置，按 300000 毫秒
  处理，填 0 则不限制超时
- **发送无缓存头**：开启后每个请求都会带上 `Cache-Control: no-cache`，
  默认关闭。请求自己已经写了这个请求头时不覆盖

### 变更

- **参数数量的算法改了**：接口详情里「参数」标签上的数字，现在是全部 Query 参数 +
  路径参数 + 本接口启用的全局参数。此前勾掉一个参数它就从计数里消失了——那个数字
  回答的是「这个接口定义了多少参数」，不是「这次会发出去几个」
- **响应区的收起手感与侧栏统一**：拖过最小高度后不再立刻消失，而是松手才决定——
  差得不够远弹回最小高度，拖够了才收起，快收起时先淡一下作预告。收起状态下的手柄上
  仍显示状态码、耗时与大小，不用为了看一眼结果再展开
- **接口树可以拖着收起**：拖到最小宽度之后还能继续往左拖——拖得不够远，松手弹回
  最小宽度；拖够了，松手就整个收起。快要收起时侧栏会先淡一下作为预告，键盘上在最小
  宽度处再按一次 ← 也是收起
- **新增参数不用先点「添加」了**：参数表与请求头表的末尾常驻一行空行，直接往里输入
  就成为正式的一行，下面再自动补出新的空行；Enter 跳到下一行的同一列，一串参数可以
  一路键盘录完。行尾的删除按钮改为鼠标移到该行时才出现，不再每行挂一个
- **语言设置并入「外观与语言」**：不再有独立的「语言」标签页，主题、界面缩放与
  语言都在同一页
- **接口的「跟随重定向」默认改为跟随全局设置**：新增的全局开关默认开启，接口上
  可以显式设为开启或关闭来覆盖它。原有接口沿用之前的行为，其中没有显式关过的
  会变成「跟随全局设置」。
  **若之后要退回 0.0.4-beta.1 及更早的版本**：旧版本不认识「跟随全局设置」这个状态，会把
  这些接口显示成「关闭」（实际请求也不再跟随重定向）；如果在旧版本里编辑保存过这些
  接口，再回到新版本时它们会变成「显式关闭」，需要手动改回「跟随全局设置」。其余
  数据不受影响

### 修复

- **后台任务出错不会再带走整个应用**：WebSocket 收发、流式响应推送、脚本里的
  `pm.sendRequest`、更新检查这些后台任务万一崩了，现在只记一条带堆栈的日志，
  应用继续运行
- **接口操作失败时日志里不再空空如也**：正式发布的包此前会丢掉框架侧的日志，
  其中包括被接住的内部错误与堆栈；现在它们和应用日志写在一起，反馈问题时有据可查
- **单个日志文件不会再无限膨胀**：日志按天切分的基础上加了 8 MiB 的大小上限，
  超出后滚动成 `postpigeon-<日期>.1.log` 这样的分片，仍然只保留 30 天
- **界面状态读坏了不再白屏**：已打开的标签页、当前环境等状态存在本地，此前跨版本
  升级后结构对不上会直接抛错、界面开不出来；现在读回来先校验，对不上就回到默认值
- **同一台机器上不会再跑起两个 PostPigeon**：重复启动会把已经打开的窗口叫到前面。
  此前两个实例共用同一个数据库文件，设置、窗口位置、Cookie 会互相覆盖，升级时还会
  各自跑一遍数据库迁移
- **启动失败不再是「双击没反应」**：配置、日志或数据库初始化失败时会弹出系统对话
  框说明原因，并给出数据目录、日志目录，以及用自动备份恢复的办法

## [0.0.4-beta.1] - 2026-08-26

### 变更

- **macOS 版本现在经过 Apple 签名与公证**：下载后可以直接双击打开，不再出现
  「已损坏，应移到废纸篓」，也不必再右键打开或手动去掉隔离属性。已安装的旧版本
  自动更新到本版后同样生效

## [0.0.3] - 2026-08-26

### 变更

- 本版本不包含面向使用者的功能变更，应用代码与 0.0.2 相同。改动集中在 CI：
  构建与发布流水线整体提速三到四成，其中 Linux 打包从 6 分 38 秒降到 3 分 06 秒
  （Go 构建缓存改为按负载分键、并随 go.sum 滚动更新），发版流程本身也借这一版
  做一次实跑验证

## [0.0.2] - 2026-08-26

### 变更

- 「关于」里的构建时间补上时区，如 `2026/08/26 15:52:59 GMT+8`。构建时间由 CI
  以 UTC 记录、展示时转成本地时区，不标时区就没法判断它到底是哪儿的时间

## [0.0.1] - 2026-08-26

### 新增

- **应用内自动更新**：从 GitHub Releases 检查、下载、校验并原地安装新版本，
  重启后生效。默认每 6 小时后台检查一次，只检查不自动下载，是否更新由用户决定；
  可关闭自动检查、切换到预发布通道、跳过指定版本。下载完成后按发布产物附带的
  `SHA256SUMS` 校验摘要，不匹配则拒绝安装。
  包管理器安装的副本（deb/rpm）与 AppImage 不做原地替换，只提示新版本并引导到
  下载页；开发构建不参与更新
- **跨版本更新说明**：跨多个版本升级时，更新面板会列出当前版本到新版本之间
  每一个版本的变更内容，而不只是最新一版。设置里也能查看完整的历史更新日志
- **集合运行器**：按模块或文件夹批量运行接口，支持多轮、请求间隔、失败即停；
  实时进度与断言汇总，报告可导出 Markdown / JSON
- **Cookie 管理**：按项目持久化 Cookie，登录态自动带到后续请求；
  项目设置内可查看、手工增删、清理过期与一键清空
- **cURL 双向互转**：从 cURL 命令新建请求（兼容浏览器「Copy as cURL」），
  以及把当前请求复制为可直接执行的 cURL
- **Postman Collection v2.x 导入**与 **OpenAPI 3.1 导出**
- **GraphQL 请求体**：查询与变量分栏编辑，发送时组装为标准的
  GraphQL over HTTP JSON；cURL / OpenAPI 导出与 Postman 导入同步支持
- **TLS 设置**（接口 → 项目 → 全局三级）：跳过证书校验、自定义 CA、
  客户端证书（双向 TLS）、最低 TLS 版本
- **Digest 与 OAuth 2.0 认证**（client_credentials / password 授权，令牌自动缓存）
- **请求取消**：进行中的请求可随时中断，不必等到超时
- **限额与保留策略**：响应体上限、入库体积上限、WebSocket 单帧上限，
  以及请求历史的保留天数与条数上限
- **全局轻提示与错误边界**：所有失败路径都有可见反馈，渲染异常不再白屏
- 响应体可另存为文件；二进制响应按原始字节写出

### 变更

- 后端错误改为「错误码 + 参数」的结构化形式，文案由前端 i18n 渲染，
  英文界面不再出现中文错误
- 请求历史默认对 Authorization / Cookie / API Key 请求头与秘密变量值脱敏
- 窗口状态重置新增 `POSTPIGEON_RESET_WINDOW=1` 与 `--reset-window` 两种方式
  （Linux/Wayland 下按 Shift 无效）
- 发版流程新增版本号一致性校验：`build/config.yml` 的版本号现在提交进仓库并与
  git tag 保持一致，由 `.githooks/pre-push`（本地）与 release 工作流的第一个
  job（CI，硬性）两处校验，对不上就不构建。新增
  `wails3 task release:prepare -- 1.2.0` 一键对齐版本号与变更日志并提交
- 发布产物改用 `PostPigeon-<平台>-<架构>` 命名，压缩包只含单个顶层条目
  （macOS 为 `.app` 的 zip、Linux 为二进制的 tar.gz、Windows 为单个 exe），
  这是自动更新能原地替换的前提；NSIS 安装包、deb/rpm/AppImage 改用独立文件名，
  仅作首次安装渠道。Release 新增 `SHA256SUMS` 与 `CHANGELOG.md` 两个资产
- 升级到 Wails v3.0.0-beta.13。Linux 桌面栈随上游从 GTK3/WebKit2GTK 4.1
  切到 GTK4/WebKitGTK 6.0（GTK3 在上游 v3.1 会移除），运行时依赖改为
  `libgtk-4-1` 与 `libwebkitgtk-6.0-4`，最低发行版要求抬到
  Ubuntu 24.04 / Debian 13；macOS 最低版本由 10.15 抬到 12.0

### 修复

- HTTP 连接现在可跨请求复用，并正确协商 HTTP/2（此前每次请求都新建
  Transport，连接从不复用且始终退回 HTTP/1.1）
- 同一接口开多个流式标签页时，先开的流不再被后开的覆盖而无法停止
- 新建接口时不再丢失类型、状态、标签、描述与前后置脚本
- 端点自带的脚本在没有配置「操作」时不再被忽略
- 下拉框中值为空字符串的选项（「无」「默认」等）现在可以正常选中与显示
- 修复应用初始化与全屏状态读取的未捕获 Promise 拒绝
- WebSocket 二进制帧不再被错误地按文本解码
- macOS 的 platform 包补上 framework 链接声明，此前无法单独构建与测试

### 安全

- WebSocket 单帧大小恢复为可配上限（此前完全不限，超大帧可打满内存）
- 响应体读取受限额约束，不再把任意大小的响应整个读进内存并写入数据库
